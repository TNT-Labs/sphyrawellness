# ========================================
# Sphyra Wellness Lab - Cloudflare Quick Tunnel
# ========================================
# SOLUZIONE PIÙ SEMPLICE: Nessun dominio o DNS richiesto!
# Genera automaticamente un URL tipo: sphyrawellness-xxx.trycloudflare.com

# ========================================
# CONFIGURAZIONE DATABASE
# ========================================
COUCHDB_USER=admin
# IMPORTANTE: Cambia questa password!
COUCHDB_PASSWORD=your-secure-password-here

# ========================================
# CONFIGURAZIONE BACKEND
# ========================================
# JWT Secret per autenticazione (OBBLIGATORIO)
# Genera una stringa casuale sicura
JWT_SECRET=your-jwt-secret-here-change-me

# SendGrid per invio email (opzionale)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@sphyrawellness.com
SENDGRID_FROM_NAME="Sphyra Wellness Lab"

# Orario invio promemoria giornalieri (formato 24h)
REMINDER_SEND_HOUR=10
REMINDER_SEND_MINUTE=0

# ========================================
# CONFIGURAZIONE FRONTEND
# ========================================
# Password iniziale admin (opzionale, default: admin123)
VITE_ADMIN_INITIAL_PASSWORD=admin123

# ========================================
# NOTE IMPORTANTI - CLOUDFLARE QUICK TUNNEL
# ========================================
# ✅ VANTAGGI:
# 1. NESSUNA configurazione DNS necessaria
# 2. NESSUN token richiesto
# 3. NESSUN dominio personalizzato necessario
# 4. Setup in 2 minuti!
# 5. URL generato automaticamente: tuonome-xxx.trycloudflare.com
#
# ⚠️ LIMITAZIONI:
# 1. URL casuale ad ogni riavvio (non permanente)
# 2. Ideale per test, demo, uso temporaneo
# 3. Per produzione: usa .env.cloudflare.example con dominio personalizzato
#
# 📋 REQUISITI:
# 1. Docker + Docker Compose
# 2. Connessione Internet (solo outbound)
#
# ⚙️ QUICK START:
# 1. cp .env.quicktunnel.example .env
# 2. Modifica password in .env
# 3. ./deploy-quicktunnel.sh
# 4. Leggi l'URL generato nei log!
