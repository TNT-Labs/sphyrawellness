# Docker Environment Variables - Sphyra Wellness Lab
# Copy this file to .env.docker and customize the values

# ============================================================================
# DATABASE (PostgreSQL)
# ============================================================================
POSTGRES_DB=sphyra_wellness
POSTGRES_USER=sphyra_user
POSTGRES_PASSWORD=sphyra_dev_password_2024

# ============================================================================
# BACKEND (Node.js + Express + Prisma)
# ============================================================================

# JWT Authentication
JWT_SECRET=sphyra_wellness_jwt_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# CORS - Allowed Origins (comma separated)
ALLOWED_ORIGINS=http://localhost,http://localhost:5173,http://localhost:80

# Email Settings (SendGrid)
SENDGRID_API_KEY=
EMAIL_FROM=noreply@sphyrawellness.local

# SMS Settings (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# ============================================================================
# FRONTEND (React + Vite)
# ============================================================================
VITE_API_URL=http://localhost:3001/api
FRONTEND_PORT=80

# ============================================================================
# PGADMIN (Database Management GUI)
# ============================================================================
PGADMIN_EMAIL=admin@sphyrawellness.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ============================================================================
# NOTES:
# - For production, change all passwords and secrets!
# - pgAdmin is optional, start with: docker compose --profile debug up
# - Default login: admin / admin123 (created by seed script)
# ============================================================================
