# ========================================
# Sphyra Wellness Lab - Configurazione Let's Encrypt
# ========================================
# Copia questo file in .env e modifica i valori

# ========================================
# CONFIGURAZIONE DOMINIO E EMAIL
# ========================================
# Il tuo dominio pubblico (OBBLIGATORIO)
# IMPORTANTE: Il dominio DEVE puntare all'IP pubblico del server
DOMAIN=esempio.com

# La tua email per le notifiche Let's Encrypt (OBBLIGATORIO)
# Riceverai notifiche di scadenza certificati
EMAIL=tua@email.com

# Modalità staging (0 = production, 1 = staging/test)
# Usa 1 per test, 0 per certificati validi in produzione
STAGING=0

# ========================================
# CONFIGURAZIONE DATABASE
# ========================================
COUCHDB_USER=admin
# IMPORTANTE: Cambia questa password!
COUCHDB_PASSWORD=your-secure-password-here

# ========================================
# CONFIGURAZIONE BACKEND
# ========================================
# JWT Secret per autenticazione (OBBLIGATORIO)
# Genera una stringa casuale sicura
JWT_SECRET=your-jwt-secret-here-change-me

# SendGrid per invio email (opzionale)
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@esempio.com
SENDGRID_FROM_NAME=Sphyra Wellness Lab

# Orario invio promemoria giornalieri (formato 24h)
REMINDER_SEND_HOUR=10
REMINDER_SEND_MINUTE=0

# ========================================
# CONFIGURAZIONE FRONTEND
# ========================================
# Password iniziale admin (opzionale, default: admin123)
VITE_ADMIN_INITIAL_PASSWORD=admin123

# ========================================
# NOTE IMPORTANTI
# ========================================
# 1. Il dominio DEVE essere pubblicamente accessibile
# 2. Le porte 80 e 443 DEVONO essere aperte
# 3. Il dominio DEVE puntare all'IP pubblico del server
# 4. I certificati Let's Encrypt scadono dopo 90 giorni
# 5. Il rinnovo è automatico ogni 12 ore
